{
  "name": "jablog-root",
  "version": "1.0.0",
  "description": "Root package for Jablog project orchestration",
  "private": true,
  "scripts": {
    "setup": "docker-compose up -d db && npm install && npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && npm install && npx prisma migrate dev && npm run seed",
    "setup:frontend": "cd frontend && npm install",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm run dev",
    "seed": "cd backend && npm run seed",
    "test": "cd backend && npm run test",
    "clean": "rm -rf node_modules backend/node_modules frontend/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
